#!/usr/bin/env python3
import sys
print("STEP 2: Python funciona", file=sys.stdout, flush=True)
print(f"Python version: {sys.version}", file=sys.stdout, flush=True)
print(f"Executable: {sys.executable}", file=sys.stdout, flush=True)
